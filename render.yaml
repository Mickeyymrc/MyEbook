services:
  - type: web
    name: myebook
    runtime: php
    buildCommand: |
      composer install --no-dev --optimize-autoloader
      php artisan optimize
      php artisan migrate
    startCommand: php artisan serve
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false